import { IIntroductionWrap } from "./interface";
import DeviceSelect from "./DeviceSelect";
import Auth from "./Auth";

const items: IIntroductionWrap[] = [DeviceSelect, Auth];

export default items;
